# Topic 17: Context API (Theme & Auth)

## Key Concepts
- `React.createContext` creates context object.
- `<Context.Provider value={...}>` wraps children to supply values.
- `useContext(Context)` reads current context value.