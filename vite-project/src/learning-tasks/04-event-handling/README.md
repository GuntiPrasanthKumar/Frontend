# Topic 04: Event Handling & Synthetic Events

## Key Concepts
- React uses camelCase event naming (`onClick`, `onKeyDown`, `onMouseMove`).
- SyntheticEvent ensures identical behavior across all web browsers.
- Handling mouse coordinates, key events, and element focus.