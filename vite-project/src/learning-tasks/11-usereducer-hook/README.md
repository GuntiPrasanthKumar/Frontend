# Topic 11: Complex State with useReducer

## Key Concepts
- Reducer function `(state, action) => newState`.
- Decoupling state logic from UI event handlers via `dispatch({ type, payload })`.
- Architectural foundation for Redux.