# Topic 05: List Rendering & Stable Keys

## Key Concepts
- Use `.map()` to transform array items into JSX.
- `key` prop helps React identify which items have changed, added, or removed.
- Avoid using array indexes as keys when list items can be reordered or removed.