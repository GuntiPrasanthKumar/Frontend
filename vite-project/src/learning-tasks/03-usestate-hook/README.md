# Topic 03: State Management with useState

## Key Concepts
- State triggers re-rendering when updated.
- Never mutate state directly; always use the setter function.
- Use functional state updates (`prev => prev + 1`) when new state depends on previous state.