# Topic 10: DOM Manipulation with useRef

## Key Concepts
- Accessing native DOM elements (e.g. input focus, scroll position, audio playback).
- Storing mutable instance variables that persist across renders without causing re-renders.